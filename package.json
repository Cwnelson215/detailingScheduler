{
  "name": "detailing-infra",
  "version": "1.0.0",
  "description": "Infrastructure for detailing",
  "main": "index.ts",
  "scripts": {
    "build": "tsc",
    "dev": "cd src && npm run dev",
    "preview": "pulumi preview",
    "up": "pulumi up",
    "destroy": "pulumi destroy"
  },
  "dependencies": {
    "@pulumi/aws": "^6.0.0",
    "@pulumi/pulumi": "^3.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}
