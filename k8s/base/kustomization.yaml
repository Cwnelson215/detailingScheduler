apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml
  - ingress.yaml
  - certificate.yaml
  - reminder-cronjob.yaml

labels:
  - includeSelectors: false
    pairs:
      app: detailing
